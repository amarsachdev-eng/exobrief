[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3.11 -m pip install anthropic requests supabase sendgrid schedule python-dotenv"]

[start]
cmd = "python3.11 main.py"
